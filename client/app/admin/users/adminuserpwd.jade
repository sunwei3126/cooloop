.col-sm-6
  form(name='passwordForm')
    alert(ng-repeat='alert in alerts', type='{{alert.type}}', close='closeAlert($index)') {{alert.msg}}
    .form-group(ng-class="{'has-error': hasError(passwordForm.newPassword)}")
      label.control-label(for='newPassword') New Password:
      input#newPassword.form-control(type='password', name='newPassword', ng-model='pass.newPassword', required='')
      span.help-block(ng-show="showError(passwordForm.newPassword, 'required')") This field is required

    .form-group(ng-class="{'has-error': hasError(passwordForm.confirm)}")
      label.control-label(for='confirm') Confirm Password:
      input#confirm.form-control(type='password', name='confirm', ng-model='pass.confirm', required='')
      span.help-block(ng-show="showError(passwordForm.confirm, 'required')") This field is required

    .form-group
      button.btn.btn-primary.btn-password(type='button', ng-disabled='!canSave(passwordForm)', ng-click='submit(passwordForm)') Set Password          
